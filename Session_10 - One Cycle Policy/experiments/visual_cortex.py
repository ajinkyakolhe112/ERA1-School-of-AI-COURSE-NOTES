import torch.nn as nn

class VisualCortexNN(nn.Module):
	def __init__(self):
		super().__init__()
		
		self.lgn = nn.ModuleDict({
		})
		
		self.v1 = nn.ModuleDict({
		})
		
		self.v2 = nn.ModuleDict({
		})
		
		self.v3 = nn.ModuleDict({
		})
		
		self.v4 = nn.ModuleDict({
		})
		
		self.pfc = nn.ModuleDict({
		})
		
	def forward(m,input_batch):
		m.v1
		
		
		return final_result
	
